/* ===========================================================================
 *  BRC — إعداد الاتصال بـ Supabase
 *  ---------------------------------------------------------------------------
 *  ⚠️ هذا الملف يُنشر مع الواجهة، فالمحتوى هنا **عامّ بحكم التصميم**:
 *    • Project URL      → ظاهر لأي زائر
 *    • Publishable key  → مفتاح عام مصمَّم للنشر (بديل anon القديم)
 *
 *  🔴 لا تضع هنا أبداً:
 *    • Secret key  (sb_secret_…) — تتجاوز كل سياسات RLS
 *    • كلمة مرور قاعدة البيانات
 *    • app.brc_secret (سرّ توقيع البصمة)
 *    كلها تُضبط في Supabase أو متغيّرات بيئة على الخادم، لا في كود الواجهة.
 *
 *  الأمان الحقيقي يأتي من سياسات RLS في docs/schema.sql — لا من إخفاء المفتاح.
 * =========================================================================== */
(function (root) {
  'use strict';

  root.BRCSupabaseConfig = {
    /* مشروع شركة بابل للتوظيف — منطقة فرانكفورت */
    url: 'https://vqsvztudvfukyuerzcgx.supabase.co',

    /* المفتاح العام (Publishable) — آمن للعرض في المتصفح */
    publishableKey: 'sb_publishable_T4tcGsQpLKJz7GoTnn8uKQ_ORLYKDCC',

    /* كل كائنات المشروع تعيش في سكيما brc منفصلة عن public */
    schema: 'brc',

    /* فعّال؟ يُستخدم للتبديل بين الوضع المحلي والسحابي أثناء الترحيل التدريجي */
    enabled: true,

    /* =======================================================================
     *  enforceAuth — هل الدخول من القاعدة حصراً؟
     *  -----------------------------------------------------------------------
     *  false (الوضع الانتقالي، وهو الحالي): تُجرَّب القاعدة أولاً، وإن لم يوجد
     *      حساب موظف بعد يعمل الدخول المحلي **بتنبيه ظاهر** حتى لا تتوقف
     *      المنظومة. البيانات في هذه الحالة تبقى في المتصفح ولا تُدفع للقاعدة
     *      (الكتابة تحتاج جلسة موظف، وهو الصحيح).
     *  true  (الوضع النهائي): لا دخول إلا من Supabase Auth وصف brc.staff.
     *      كلمات مرور config.js تصبح بلا أثر، ويختفي الباب الخلفي المنشور.
     *
     *  ⚠️ اجعلها true **فقط** بعد إنشاء حساب Auth لكل موظف وربطه بسطر في
     *     brc.staff (انظر docs/supabase-setup.md §6). لو فعّلتها قبل ذلك
     *     فلن يستطيع أحد الدخول — وهذا هو المقصود أمنياً، لكنه يوقف العمل.
     * ======================================================================= */
    enforceAuth: true
  };
})(typeof window !== 'undefined' ? window : globalThis);
